/*#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(void)
{
    char *str = (char*) malloc(10);
    gets(str);

    char *ptr = (char*) malloc(strlen(str + 1));
    strcpy(ptr, str);
    printf("%s", ptr);
    free(ptr);
    free(str);
}*/

